<h1>Choose Your Faction!</h1>
<p>
  To participate in the Quest, you must first choose a faction with which to side.
  Pick the faction that best fits you from the list below -- and be aware that
  changing sides during the game is possible.
</p>

<% @factions.each do |faction| %>
  <div class="row">
    <div class="span3">
      <%= image_tag(faction.icon.url, alt: "#{faction.name} faction badge") if faction.icon %>
    </div>
    <div class="span4">
      <h2><%= faction.name %></h2>
      <p><%= faction.description %></p>
      <%= link_to "Choose #{faction.name}", choose_faction_user_path(@user, faction_id: faction.id), class: "btn btn-success" %>
    </div>
  </div>
<% end %>
